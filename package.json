{
  "name": "smartpos-root",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npx prisma db push --accept-data-loss && node prisma/seed.js && node src/index.js"
  }
}
